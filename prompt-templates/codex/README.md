# Prompt Templates Moved

Templates in this directory were migrated.

Use:
- `prompt-templates/base/` for task-type base templates
- `prompt-templates/workers/` for worker profiles

This directory is kept only as a compatibility pointer.
